{
  "name": "es6katas.org",
  "version": "0.0.1",
  "description": "es6katas.org site",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rm -Rf dist && mkdir dist && cp src/index.html dist",
    "build": "browserify src/index.js --outfile=dist/index.js --transform=babelify",
    "watch": "watchify src/index.js --outfile=dist/index.js --transform=babelify -v"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/tddbin/es6katas.org.git"
  },
  "keywords": [
    "kata",
    "es6"
  ],
  "author": "uxebu",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/tddbin/es6katas.org/issues"
  },
  "homepage": "https://github.com/tddbin/es6katas.org",
  "devDependencies": {
    "atomic": "0.0.2",
    "browserify": "^9.0.3",
    "babelify": "^5.0.4"
  }
}
